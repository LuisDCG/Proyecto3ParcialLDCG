<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"

        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrarEmpleadosActivity" />
        <activity android:name=".InicioActivity" />
        <activity android:name=".MenuDesayunosActivity" />
        <activity android:name=".MenuComidasActivity" />
        <activity android:name=".MenuCenasActivity" />
        <activity android:name=".MenuAntojosActivity" />
        <activity android:name=".CocineroActivity" />
        <activity android:name=".MeseroActivity" />
        <activity android:name=".CambiarContraActivity" />
        <activity android:name=".CajadecobroActivity" />
        <activity android:name=".MesasActivity" />
    </application>

</manifest>